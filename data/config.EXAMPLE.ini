[Auth]
# Your Discord bot token
bot_token =

[TriggerConfig]
# Which guild to act on (only one supported for now)
guild_id =
# In which channel the triggering message is
auth_trigger_channel_id =
# Which message triggers the authentication (this should probably be a rules channel or similar)
auth_trigger_message_id =
# Which emoji to look for on the above message
auth_trigger_emoji = 🧠

[AuthConfig]
# Where to create the new authentication channels
auth_channel_category_id =
# What role to add when successfully authenticated
auth_success_role_id =